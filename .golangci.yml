version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - misspell
    - unused
  settings:
    revive:
      rules:
        - name: exported
          disabled: false
    misspell:
      locale: US

formatters:
  enable:
    - gofumpt
